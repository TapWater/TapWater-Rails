<p id="notice"><%= notice %></p>

<h1>Listing drinks</h1>

<table>
  <thead>
    <tr>
      <th>Category</th>
      <th>User</th>
      <th>Uuid</th>
      <th>Drink date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @drinks.each do |drink| %>
      <tr>
        <td><%= drink.category %></td>
        <td><%= drink.user_id %></td>
        <td><%= drink.uuid %></td>
        <td><%= drink.drink_date %></td>
        <td><%= link_to 'Show', drink %></td>
        <td><%= link_to 'Edit', edit_drink_path(drink) %></td>
        <td><%= link_to 'Destroy', drink, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Drink', new_drink_path %>
<%= link_to 'New User', new_user_path %> <br>
<%= link_to 'Register New Admin', new_admin_path %>
<%= link_to 'Log Out', session_path(current_admin.id), method: :delete, data: { confirm: "Log out?" } %>
